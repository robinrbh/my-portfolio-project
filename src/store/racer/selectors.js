export const selectRacerName = (state) => state.racer.firstName

export const selectRacerToken = (state) => state.racer.token

export const selectRacerId = (state) => state.racer.id

// export const selectStSubjects = (state) => state.student.subjects;
